CREATE SCHEMA `patients_db` DEFAULT CHARACTER SET utf8 COLLATE utf8_unicode_ci ;
drop table patients;
create table if not exists patients(
id int NOT NULL AUTO_INCREMENT,
email varchar(45),
name varchar(45),
address varchar(45),
birth_date varchar(10),
PRIMARY KEY(id)
);